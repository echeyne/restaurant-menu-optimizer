Effect: Allow
Action:
  - s3:PutObject
  - s3:GetObject
  - s3:DeleteObject
  - s3:ListBucket
Resource:
  - arn:aws:s3:::${self:provider.stage}-menu-files-${self:provider.region}
  - arn:aws:s3:::${self:provider.stage}-menu-files-${self:provider.region}/*