custom:
  userPoolId: ${cf:${self:service}-${self:provider.stage}.UserPoolId}
  clientId: ${cf:${self:service}-${self:provider.stage}.UserPoolClientId}
  menuFilesBucket: ${cf:${self:service}-${self:provider.stage}.MenuFilesBucketName}